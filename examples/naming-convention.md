# Naming convention starter

Suggested pattern:

`<org>-<workload>-<environment>-<region>-<sequence>`

Keep names deterministic, policy-friendly and easy to map to monitoring, cost and ownership data.
