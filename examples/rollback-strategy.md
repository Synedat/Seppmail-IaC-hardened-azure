# Rollback strategy

- Capture the deployed template/module version
- Export the current state or ARM deployment record
- Tag the change window and approver
- Retain previous parameter files
- Validate recovery paths before production rollout
