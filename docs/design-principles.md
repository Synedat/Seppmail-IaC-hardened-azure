# Design principles

This starter repository is intentionally small and focuses on a few hardened building blocks.

Recommended next steps:
- add Key Vault backed secrets and certificate handling
- integrate Azure Monitor diagnostics and alerts
- split networking, compute and policy into reusable modules
- encode organizational naming, RBAC and tagging standards
- add CI validation for fmt, validate, tflint and checkov or similar tools
